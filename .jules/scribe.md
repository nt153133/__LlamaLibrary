# Scribe's Journal
## 2026-05-22 - [BagSlotExtensions] Learning: High-quality documentation for memory-driven extension methods requires tracing the logic of the underlying memory calls (e.g., HQ ID offsets). Action: Always verify the raw ID logic when documenting item-handling methods.
## 2026-05-23 - [LlamaManagers] Learning: Racing Chocobo stats (Speed, Accel, etc.) are raw points, not final values. Final stats use formula: ((Pedigree + Stars) * 0.38) * Value. Action: Include domain-specific formulas and bit-field breakdowns (Sex/Weather/Pedigree) in <remarks> to aid developers.
## 2026-05-24 - [RetainerItemFinder] Learning: The game's internal item finder uses a binary tree structure for caching retainer inventories. Action: When documenting memory-mapped trees, clarify the recursive traversal logic (e.g., in `ItemFinder.Visit`) and the specific byte-offsets for item arrays and crystals to ensure clarity on how raw memory is parsed into structured inventory objects.
## 2026-06-07 - [SettingsBase] Learning: Persistent settings use a 500ms debounced save mechanism and reflection-based wiring for collection change events. Action: Ensure documentation for settings classes explains the automatic persistence and UI thread synchronization requirements.
## 2026-06-12 - [CustomDeliveries] Learning: The CustomDelivery struct is a fixed-size (0x3C) structure where reward arrays (Gil, Exp, Scrip) align with three collectability tiers (0: Acceptable, 1: Mid, 2: Maximum). Action: Ensure documentation for satisfaction supply members explicitly mentions this three-tier alignment to clarify how reward indices map to in-game performance.
## 2026-06-25 - [UIState] Learning: Minion unlock status is stored in a 0x50-byte bit-array where each byte represents 8 minions. Action: Always include the specific bit-mask logic (1 << (id & 7)) & array[id >> 3] in <remarks> for bit-field lookups to improve clarity.
## 2026-07-01 - [SquadronStatus] Learning: Grand Company Squadron status (mission/training) is memory-mapped as Unix timestamps and uses a 1-minute caching TTL. Action: Ensure documentation for squadron timers mentions both the Unix-to-Local conversion and the specific cache period to set accurate developer expectations for data freshness.
## 2026-07-15 - [ProductPatterns] Learning: Multi-region memory support is implemented via a central registry that maps regional identifiers (Global, China, Korea) to product-specific pattern dictionaries. Action: When documenting pattern-related types, clarify that these strings are raw signatures used by the assembly loader to resolve memory offsets across different game versions.
## 2026-06-24 - [RemoteAgents] Learning: "Dawn" is the internal game name for the Duty Support and Trust systems. Action: When documenting "Dawn" related agents or offsets, explicitly reference both Duty Support and Trust to ensure clarity for developers familiar with the in-game terminology.
